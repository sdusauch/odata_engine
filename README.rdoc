=== OData Engine

This is a Rails Engine that enables exposing available Active Record models through an OData service endpoint.

The code is (heavily) based on the OdataOnRails plugin by Mark Borkum and is a work in progress.
